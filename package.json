{
  "name": "code-archaeologist-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Unified workspace for Code Archaeologist frontend and backend",
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "install:backend": "echo 'Please ensure your Python environment is active and run: pip install -r backend/requirements.txt'",
    "install:all": "npm run install:frontend",
    "start:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && python app.py",
    "start": "concurrently --kill-others \"npm run start:frontend\" \"npm run start:backend\"",
    "dev": "npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}